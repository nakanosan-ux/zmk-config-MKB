# Basic hardware configurations
CONFIG_SPI=y
CONFIG_INPUT=y
CONFIG_ZMK_POINTING=y

# PMW3610 (badjeff ALT driver)
CONFIG_PMW3610_ALT=y

# これは device tree で enabled になる必要があるので、conf ではなく overlay を直すのが本筋
# CONFIG_ZMK_INPUT_PROCESSOR_KEYBIND=y   ← これは一旦残してもいいが、DT側が status="okay" じゃないと n に落ちる
